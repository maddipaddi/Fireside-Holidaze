export const BASE_URL = `https://v2.api.noroff.dev`;

export const AUTH = `${BASE_URL}/auth`;

export const REGISTER = `${AUTH}/register`;

export const LOGIN = `${AUTH}/login`;

export const HOLIDAZE = `${BASE_URL}/holidaze`;

export const VENUES = `${HOLIDAZE}/venues`;

export const PROFILE = `${HOLIDAZE}/profiles`;

export const SINGLE_VENUE = `${VENUES}`;

export const SINGLE_BOOKING = `${HOLIDAZE}/bookings`;
